from setuptools import setup, find_packages

setup(
    name="math_functions",
    version="1.0.0",
    description="Un package Python pour des fonctions mathématiques",
    author="Hasna Wahi",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.6",
)
